packages:
  # 指定根目录直接子目录中的包
  # - 'my-app'
  # packages/ 直接子目录中的所有包
  - 'packages/*'
  # apps/ 直接子目录中的所有包
  - 'apps/*'
  # 排除测试目录中的包
  - '!**/test/**'

overrides:
  "react": "19.0.0"
  "react-dom": "19.0.0"